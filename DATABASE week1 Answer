**CREATING DATABASE SALESDB**
CREATE DATABASE salesDB;

 **DROPPING DATABASE DEMO**
In order to create the databasecalled Demo, we have to create IT first as the database does not exist
CREATE DATABASE demoDB;

Now we can drop the database
DROP DATABASE demodb;
